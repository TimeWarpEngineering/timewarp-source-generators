<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <packageSources>
    <clear />
    <add key="nuget.org" value="https://api.nuget.org/v3/index.json" protocolVersion="3" />
    <!-- Local feed omitted: NuGet fails restore (NU1301) when artifacts/packages is missing
         (fresh clone / CI). Packages still land there via PackageOutputPath for upload/push. -->
  </packageSources>
</configuration>
